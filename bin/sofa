#!/usr/bin/ruby
require 'rubygems'
begin
  require 'sofaskull'
rescue LoadError
  require File.expand_path(File.dirname(__FILE__)+'/../lib/sofaskull')
end

SofaSkull::Program.new(if i=ARGV[0]
  File.read(i)
else
  $stdin.read
end).run